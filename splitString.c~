#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char argv[])
{
  char str[] = "Let's break this string into  pieces";

  char* piece = strtok(str, " ");
  printf("%s\n", piece);
  return 0;
}
